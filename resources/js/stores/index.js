import {
    http,
    httpFile
} from '../services/http_service';

const state = () => ({
})

const mutations = {
}

const actions = {
}

export default {
    namespaced: true,
    state,
    actions,
    mutations
}